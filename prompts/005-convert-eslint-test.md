We are in the process of porting old eslint tests into our new linter.

To see how we write tests, review the @skills/writing-tests/SKILL.md document.

Use the @test/lib/rules/constructor-super.test.js test module as an example of how to write tests for rules.

Your task is to reformat the @test/lib/rules/default-case-last.test.js test module to match constructor-super.test.js and write the tests for it.

For the comma-dangle tests, you'll comment out the languageOptions so they are documented, but ignored.

Write the tests for default-case-last, and work on any bugs which are discovered, but DO NOT move on to writing tests for any other rules.
